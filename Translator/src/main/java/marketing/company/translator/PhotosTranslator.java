package marketing.company.translator;

import marketing.company.domain.dto.PhotosDto;

import java.util.List;

public interface PhotosTranslator
{
     List<PhotosDto> getAllPhotos();
   // PhotosDto getPhotosID(Long photosID);
  //PhotosDto Read(PhotosDto photos);
   // Photos Read(Photos photos);
   // void Create(Photos info);
    //void Update(Photos info);
    //void Delete(Photos info);
  // List<PhotosDto> Read();
  // List<PhotosDto> photosDto();

   //  PhotosDto Read(PhotosDto photosDto);
    // PhotosDto Create(PhotosDto photosDto);
     //PhotosDto Update(PhotosDto photosDto);
     //PhotosDto Delete(PhotosDto photosDto);
}
